import { UserUpdateValidationPipe } from './user-update-validation.pipe';

describe('UserUpdateValidationPipe', () => {
  it('should be defined', () => {
    expect(new UserUpdateValidationPipe()).toBeDefined();
  });
});
